#include "Classes.h"

Classes::Classes(const string& ClassCode, const string& UcCode, const string& Weekday, float StartHour, float Duration, const string& Type): ClassCode(ClassCode), UcCode(UcCode), Weekday(Weekday), StartHour(StartHour), Duration(Duration), Type(Type){

}
